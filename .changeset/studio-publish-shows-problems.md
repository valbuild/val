---
"@valbuild/ui": patch
---

When content refuses a Studio publish, the error now lists content's reasons, one line per problem (for example `COMMIT_INVALID: …`), instead of only "This publish cannot be declared".
