---
"@valbuild/next": patch
---

Document `s.date()` and `s.datetime()` in the schema reference. Neither was covered before, so the only way to discover them was reading the source. Both now have a section with the stored format and an example, and both are listed in the table of contents.
