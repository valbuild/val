---
"@valbuild/ui": patch
---

A literal field in an object, such as `type: s.literal("bento-box")`, now shows its value in the Studio as a read-only text field. Before, it showed the error "Literal fields are not editable".
