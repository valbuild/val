---
"@valbuild/ui": patch
---

**See what has changed on a page.** The fields view of the preview ("On page")
now marks every field with an unpublished change, and a **Changed** chip beside
the title filters the list down to just those, so you can scan a page's edits
before you publish. The chip shows how many fields have changed and fits on the
title row, so it takes no extra room on a phone. A field counts as changed
whether the edit was to the field itself, to something inside it (an image's
alt text, a paragraph of rich text) or to the object or list item it sits in.
