# next.js
examples/next*/.next/
examples/next*/.next-http/
examples/next*/out/

# production
examples/next*/build

trials

dist
tsconfig.tsbuildinfo

packages/ui/server/.tmp
out

.tmp
examples/next*/.val/*

pnpm-lock.yaml

# Unzipped `val debug` snapshots: customer source, not ours to format.
debug/*/

# Git worktrees checked out inside the repo (Claude Code puts them here).
# `prettier --check .` walks the whole tree, so a second checkout means the
# format gate reports on someone else's in-progress work — and `--write` would
# reformat it under them.
.claude/worktrees/
