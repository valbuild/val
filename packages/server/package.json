{
  "name": "@val/server",
  "main": "dist/val-server.cjs.js",
  "version": "1.0.0",
  "scripts": {
    "dev": "ENV=development nodemon --watch 'src/**' --watch '.env' --ext 'js,ts,tsx,.env,json' --exec 'ts-node src/index.ts'"
  },
  "devDependencies": {
    "@types/express": "^4.17.15",
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1",
    "@val/lib": "*"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
