// TODO: we should be able to type this just remember StegaSource is one but not all of the input types
export function fetchRemoteSource(source: any): Promise<any> {
  //
}

export function nullifyRemoteSource(source: any): any {
  //
}
