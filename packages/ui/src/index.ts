export * from "./constants";
export const IS_DEV = true;
export const VERSION = "$$BUILD_$$REPLACE_WITH_VERSION$$"; // NOTE: this is included in links in development mode, but that's fine. Or at least it is OK for now.
